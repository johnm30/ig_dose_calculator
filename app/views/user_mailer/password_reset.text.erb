To reset <%= @user.name %>'s password for the Calculator App click the link below:

<%= edit_password_reset_url(@user.reset_token, name: @user.name) %>

This link will expire in two hours.

If they did not request the password to be reset, please ignore this email and
the password will stay as it is.